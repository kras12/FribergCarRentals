@using FribergCarRentals.Areas.Admin.Controllers
@using FribergCarRentals.Controllers.Customer
@using FribergCarRentals.Shared.Mvc.Helpers
@using FribergCarRentals.Shared.Models.ViewModels.Image
@using FribergCarRentals.Shared.Models.ViewModels.Other
@using FribergCarRentals.Shared.Mvc.Components
@model ListViewModel<SlideShowImageViewModel>

@{
    ViewData["Title"] = "Home Page";
}

<h1>Browse our cars</h1>
<div class="vertical-flex-container flex-container__justify-content__center flex-container__align-items-center flex-container__row-gap__1-rem">
    <div>
        @if (Model.Models.Count > 0)
        {
            @(await Component.InvokeAsync<ImageSlideShow>(new
            {
                images = Model.Models
            }))
        }
    </div>
    <p class="text-align-center no-margin">We have a fine selection of cars that you can rent right away.</p>
    <a class="button" asp-action="@(nameof(CustomerOrderController.Book))" asp-controller="@(ControllerHelper.GetControllerName<CustomerOrderController>())">Find your car</a>
</div>
